 Write a Python program to print the multiplication table of a given numbe
 def multiplication(n):
  for i in range(1,11):
    print(f"{n} x {i} = {n*i}")
n=9
multiplication(n)

Write a Python program to find the factorial of a number using a loop.
def factorial(n):
  f=1
  for i in range(1,n+1):
    f=f*i
  print(f)
n=5
factorial(5)

Write a Python program to check if a number is a palindrome or not.
def palindrome(n):
  s=0
  m=n
  while n>0:
    r=n%10
    s=r+s*10
    n=n//10
  if s==m:
    print("palindrome")
  else:
    print("not a palindrome")
n=120
palindrome(n)

 Write a Python program to find the sum of the first N natural numbers.
 def sum(n):
  s=n*(n+1)//2
  print(s)
n=10
sum(n)

Write a Python program to find the LCM of two numbers.
def lcm(a,b):
  import math
  gcd=math.gcd(a,b)
  lcm=a*b//gcd
  print(lcm)
a,b=4,6
lcm(a,b)


 Write a Python program to find the square root of a number using the Newton-Raphson method
 def square(n):
  import math
  s=math.sqrt(n)
  if s*s==n:
    print("perfect square")
  else:
    print("not a perfect square")
n=33
square(n)

Write a Python program to find the second largest element in a list
def sec_max(a):
  a.sort(reverse=True)
  sec_max=a[1]
  print(f"second largest number is {sec_max} ")
a=[1,2,5,3,6,4,9,7,8]
sec_max(a)

Write a Python program to find the greatest common divisor (GCD) of two numbers.
def gcd(a,b):
  import math
  gcd=math.gcd(a,b)
  print(gcd)
a,b=4,6
gcd(a,b)

 Write a Python function to remove duplicate elements from a list.
 def duplicate(a):
  a=list(set(a))
  print(a)
a=[1,4,4,5,2,3,45,3,5,4,7]
duplicate(a)
  
